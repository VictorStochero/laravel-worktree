---
description: Lista e inspeciona os worktrees do repositório (branch, base, URL .test, banco, estado)
---

Invoque a skill **`laravel-worktree`** e execute a **Operação B — INSPECIONAR** no repositório atual.

Apresente a tabela: pasta | branch | base | URL `.test` | banco | sujo?. Sinalize worktrees órfãos e sugira `git worktree prune` quando houver.
